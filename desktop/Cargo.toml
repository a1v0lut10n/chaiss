[package]
name = "desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.34.1" # Wait, I can let cargo add sort it out, or specify it directly. Let's just specify recent versions.
tokio = { version = "1.0", features = ["full"] }
chaiss_core = { path = "../core" }
